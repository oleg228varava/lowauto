name: autolowparts_web
description: AutoLowParts Web на Flutter
publish_to: "none"
version: 1.0.0

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.6
  provider: ^6.0.5
  intl: ^0.20.2
  shared_preferences: ^2.2.2
  image_picker: ^1.1.2
  file_picker: ^10.3.7
  crop_your_image: ^0.7.0
  crop: ^0.5.5
  translator: ^0.1.7
  flutter_localizations:
    sdk: flutter



dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  generate: true